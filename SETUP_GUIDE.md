# 🚀 Руководство по настройке Facebook CAPI и встроенной формы

## ✅ Что было добавлено:

1. **Facebook Conversions API (CAPI)** - серверная отправка событий
2. **Встроенная форма** - замена внешнего AmoCRM редиректа
3. **Lead API** - обработка заявок с интеграцией AmoCRM и Telegram
4. **Микроконверсии** - отслеживание scroll, time, clicks

---

## 📋 Шаг 1: Настройка переменных окружения в Vercel

### Перейдите в Vercel Dashboard:
```
https://vercel.com/arberika/truck-driver-landing/settings/environment-variables
```

### Добавьте следующие переменные:

#### **Обязательные:**

| Название                   | Значение                                          | Описание                        |
|----------------------------|---------------------------------------------------|---------------------------------|
| `FACEBOOK_PIXEL_ID`        | `3789700971281396`                                | ID вашего Facebook Pixel       |
| `FACEBOOK_ACCESS_TOKEN`    | `EAAHu13VhQCsB...` (ваш токен)                    | Facebook API Access Token      |

#### **Опциональные:**

| Название                   | Значение                                          | Описание                        |
|----------------------------|---------------------------------------------------|---------------------------------|
| `FACEBOOK_TEST_EVENT_CODE` | Оставить пустым или указать Test Event Code      | Для тестирования CAPI          |
| `AMOCRM_SUBDOMAIN`         | `yourcompany`                                     | Поддомен AmoCRM                |
| `AMOCRM_API_KEY`           | Bearer токен из AmoCRM                            | API ключ для AmoCRM            |
| `TELEGRAM_BOT_TOKEN`       | `123456789:ABCDEF...`                             | Токен Telegram бота            |
| `TELEGRAM_CHAT_ID`         | `-1001234567890`                                  | ID чата для уведомлений        |

### Как получить Facebook Access Token:

1. Перейдите в [Facebook Business Manager](https://business.facebook.com/)
2. Выберите ваш рекламный аккаунт
3. Перейдите в **События > Conversions API**
4. Нажмите **Generate Access Token**
5. Токен должен иметь права: `ads_management`

---

## 📋 Шаг 2: Деплой на Vercel

### Вариант A: Через командную строку

```bash
cd /tmp/ce-landing

# Добавить все изменения
git add .
git commit -m "Add Facebook CAPI and embedded form"

# Запушить в GitHub
GIT_SSH_COMMAND="ssh -i ~/.ssh/arberika_key" git push origin main

# Vercel автоматически задеплоит изменения
```

### Вариант B: Через Vercel Dashboard

1. Перейдите на https://vercel.com/arberika/truck-driver-landing
2. Нажмите **Deploy**
3. Vercel автоматически задеплоит последний коммит из GitHub

---

## 📋 Шаг 3: Проверка работы CAPI

### Тестирование через Test Events:

1. Перейдите в **Facebook Events Manager**
2. Выберите ваш Pixel (3789700971281396)
3. Перейдите в **Test Events**
4. В `.env` добавьте `FACEBOOK_TEST_EVENT_CODE` (из Test Events)
5. Откройте сайт и выполните действие (заполните форму)
6. В Test Events должно появиться событие с двух источников:
   - **Browser Pixel** (🌐)
   - **Conversions API** (🖥️)

### Проверка дедупликации:

Оба события должны иметь **одинаковый event_id** — это гарантирует, что Facebook не задублирует событие.

---

## 📋 Шаг 4: Настройка AmoCRM (опционально)

### Получение API ключа AmoCRM:

1. Войдите в AmoCRM
2. Перейдите в **Настройки** → **API** → **Интеграции**
3. Создайте новую интеграцию "Landing Form"
4. Скопируйте **Access Token** (Bearer token)
5. Добавьте в Vercel переменную `AMOCRM_API_KEY`

### Получение Field IDs:

```bash
curl -X GET "https://yourcompany.amocrm.ru/api/v4/leads/custom_fields" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

Найдите ID полей:
- `PHONE`
- `EMAIL`
- `WHATSAPP`
- `PACKAGE`
- `COMMENTS`
- `UTM_SOURCE`
- `UTM_CAMPAIGN`

Обновите их в `/api/submit-lead.js` (строки 47-54)

---

## 📋 Шаг 5: Настройка Telegram уведомлений (опционально)

### Создание бота:

1. Откройте Telegram, найдите @BotFather
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте **Token** → добавьте в `TELEGRAM_BOT_TOKEN`

### Получение Chat ID:

1. Добавьте бота в группу или канал
2. Отправьте любое сообщение в группу
3. Откройте: `https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates`
4. Найдите `"chat":{"id":-1001234567890}` → это ваш `TELEGRAM_CHAT_ID`

---

## 🧪 Тестирование всех функций

### 1. Тест Facebook CAPI:

```bash
curl -X POST https://ce.woweri.com/api/facebook-capi \
  -H "Content-Type: application/json" \
  -d '{
    "event_name": "PageView",
    "event_id": "test_12345",
    "event_data": {
      "page_url": "https://ce.woweri.com/",
      "site_language": "ru"
    },
    "user_data": {}
  }'
```

Ожидаемый ответ:
```json
{
  "success": true,
  "events_received": 1,
  "fbtrace_id": "...",
  "event_id": "test_12345"
}
```

### 2. Тест Lead API:

```bash
curl -X POST https://ce.woweri.com/api/submit-lead \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тест Тестов",
    "phone": "+77777777777",
    "whatsapp": "+77777777777",
    "email": "test@example.com",
    "package_type": "premium",
    "page_url": "https://ce.woweri.com/",
    "site_language": "ru",
    "user_id": "test_user_123",
    "session_id": "test_session_456"
  }'
```

Ожидаемый ответ:
```json
{
  "success": true,
  "lead_id": 12345,
  "fb_event_id": "...",
  "message": "Заявка успешно отправлена!"
}
```

---

## 📊 Мониторинг и отладка

### Просмотр логов Vercel:

```bash
vercel logs --prod
```

### Проверка событий в Facebook:

1. **Events Manager** → ваш Pixel → **Overview**
2. Проверьте колонку **Event Source**:
   - 🌐 Browser (от JavaScript pixel)
   - 🖥️ Server (от Conversions API)

### Распространенные ошибки:

| Ошибка                      | Причина                          | Решение                                    |
|-----------------------------|----------------------------------|--------------------------------------------|
| `401 Unauthorized`          | Неверный Access Token            | Проверьте `FACEBOOK_ACCESS_TOKEN`          |
| `Missing required fields`   | Не переданы name или phone       | Проверьте форму                            |
| `CORS error`                | Блокировка браузером             | Проверьте CORS headers в API функциях      |
| `Events not deduplicated`   | Разные event_id                  | Проверьте, что используется один event_id  |

---

## 🎯 Следующие шаги

После успешного деплоя:

1. ✅ Проверьте работу формы на сайте
2. ✅ Убедитесь, что события приходят в Facebook (Browser + Server)
3. ✅ Проверьте Test Events на дедупликацию
4. ✅ Настройте AmoCRM и Telegram (если нужно)
5. ✅ Запустите тестовую рекламную кампанию
6. ✅ Мониторьте ROAS и качество данных

---

## 📞 Поддержка

Если возникли проблемы:
- Проверьте логи: `vercel logs --prod`
- Проверьте переменные окружения в Vercel Dashboard
- Убедитесь, что Access Token актуален (не истек)

**Документация:**
- [Facebook Conversions API](https://developers.facebook.com/docs/marketing-api/conversions-api)
- [Vercel Serverless Functions](https://vercel.com/docs/functions/serverless-functions)
- [AmoCRM API](https://www.amocrm.ru/developers/content/crm_platform/platform-abilities)
